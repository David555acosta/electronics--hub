spring:
  application:
    name: specialization
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 1MB
  cache:
    type: simple

springdoc:
  swagger-ui:
    path: /swagger-ui.html