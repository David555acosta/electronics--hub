version: "3.1"

services:
  spring-boot:
    build: ./
    ports:
      - "9526:9526"
    environment:
      SPRING_PROFILES_ACTIVE: dev
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9526/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5